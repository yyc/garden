#!/bin/bash -e

repo_root=$(cd `dirname $0` && cd .. && pwd)

# node  --inspect --expose-gc --stack-trace-limit=1000 --max-old-space-size=4096 --max-semi-space-size=64 ${repo_root}/cli/bin/garden "$@"
node --stack-trace-limit=1000 --max-old-space-size=4096 --max-semi-space-size=64 ${repo_root}/cli/bin/garden "$@"
